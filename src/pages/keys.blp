using Gtk 4.0;
using Adw 1;

template $VoucherKeysTab: Adw.NavigationPage {
  title: "keys";
  child: Adw.StatusPage {
    title: _("keys stuff");
    description: _("Voucher is ready to use!");
  };
}