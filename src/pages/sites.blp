using Gtk 4.0;
using Adw 1;

template $VoucherSitesTab: Adw.NavigationPage {
  title: "sites";
  child: Adw.StatusPage {
    title: _("Sites list");
    description: _("This page will allow you to set service preferences (e.g. default key for a site)");
  };
}